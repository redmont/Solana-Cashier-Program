import { FC, PropsWithChildren } from 'react';

export const {{ pascalCase name }}: FC<PropsWithChildren> = ({ children }) => {
  return (
    <div>
      <h1>{{ pascalCase name }} Component</h1>
      {children}
    </div>
  );
};
