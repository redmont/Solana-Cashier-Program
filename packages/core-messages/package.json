{
  "name": "core-messages",
  "private": true,
  "version": "1.0.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --external '@grpc/grpc-js' --external '@grpc/proto-loader' --external 'mqtt' --external 'amqplib' --external 'amqp-connection-manager' --external 'kafkajs' --external 'class-validator' --external 'class-transformer'",
    "dev": "pnpm run build -- --watch"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "broker-comms": "workspace:*",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
